/* Minimal placeholder to avoid runtime errors when Plotly is not bundled. */
(function () {
  if (window.Plotly) return;
  window.Plotly = {
    newPlot: function () {
      return Promise.resolve();
    },
    react: function () {
      return Promise.resolve();
    },
  };
})();
